[build]
  functions = "netlify/functions"
  publish = "build"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["psn-api"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
